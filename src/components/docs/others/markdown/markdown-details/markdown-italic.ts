import dedent from "dedent";

export const detail = dedent(`
    少しだけ目立たせたい語句 に斜体を使う
    \`\`\`markdown
    *斜体です*
    _斜体です_ (単語内で崩れる可能性があるため非推奨)
    ***かなり強調***     太字 + 斜体
    \`\`\`

    私は *Harry Potter* が好きです。
`);