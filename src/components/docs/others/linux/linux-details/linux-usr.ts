import dedent from "dedent";

export const detail = dedent(`
    Node.js, npm, PHP, Composer, bash など実行プログラム本体<br />
    Dockerイメージ側に含まれる
    \`\`\`txt
    /usr/bin
    /usr/lib
    /usr/share
    /usr/local/bin
    \`\`\`
`);