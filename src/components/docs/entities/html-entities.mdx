import Table from "@/components/docs/ui/Table";
import { entityColumns, entityData } from "@/components/docs/entities/entity-data";

## HTML文字参照とは？

HTMLでは、特定の文字をそのまま書くと**タグとして解釈されたり、構文が壊れることがある。
そのため、安全に表示するために「文字参照（HTMLエンティティ）」を使う。

[HTML特殊文字変換ツール](https://tech-unlimited.com/escape.html)

<Table
    data={entityData}
    columns={entityColumns}
/>
