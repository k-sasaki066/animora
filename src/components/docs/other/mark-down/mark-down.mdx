import Table from "@/components/docs/ui/Table";
import { markdownColumns, markdownData } from "./mark-down-data";
import { markdownUseColumns, markdownUseData } from "./mark-down-use-data";

### Mark Downとは？
シンプルな記号で文章を装飾できる軽量マークアップ言語

#### Mark Downの使用場面
<Table
    data={markdownUseData}
    columns={markdownUseColumns}
/>

#### 基本の書き方
<Table
    data={markdownData}
    columns={markdownColumns}
    accordion
    detailKey="detail"
/>